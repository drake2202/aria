# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environment
.venv/

# XDG runtime / user data (not part of the project)
logs/archive/

# Original Windows launcher binaries (proprietary, not redistributed)
*.exe
*.dll
*.ocx
*.dat
*.debug
*.manifest
*.qm

# Flash plugin binary (distribute separately)
flash/libpepflashplayer.so

# Build artifacts
*.AppImage
*.spec
dist/
build/

# Editor
.vscode/
*.swp
*~

# macOS
.DS_Store
.AppleDouble

# Flash plugin binaries — bundled in flash/ directory, tracked by git
# Exclude only the old root-level copy (used before bundling)
/PepperFlashPlayer.plugin/
